import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App/>);
  
 //const linkElement1 = screen.getByText("Welcome to testing");
 // expect(linkElement1).toBeInTheDocument();
});
